# House photos

Drop photos here and reference them from `data/houses.ts` via the `image` field:

    { number: 1, name: "Kāpa", ..., image: "/houses/01.jpg" }

Recommended: landscape orientation, at least 1200px wide, JPG. Cards crop to 4:3.
Any house without an `image` keeps the illustrated numbered tile.
